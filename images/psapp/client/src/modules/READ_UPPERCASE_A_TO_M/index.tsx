import {ModuleBuilder} from '@src/modules/common/READ_LETTERS/ModuleBuilder';

export default ModuleBuilder({
  variants: ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J', 'K', 'L', 'M'],
  maxScorePerVariant: 2,
});
