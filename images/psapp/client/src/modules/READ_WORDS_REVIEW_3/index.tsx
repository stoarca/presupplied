import React from 'react';

import {
  ModuleBuilder
} from '@src/modules/common/READ_WORDS_REVIEW/ModuleBuilder';

export default (props: never) => {
  return (
    <ModuleBuilder reviewModule__dirname={__dirname}/>
  );
};

